# 短剧解说剪辑 Codex 对话初始化

## 远程 Agent 能力验证

网页网关模式必须先创建一个不继承当前对话历史的真实子 Agent，并让它原样
返回最新的探针 nonce。随后向能力验证接口提交以下精确结构：

```json
{
  "probe_nonce": "<最新 probe nonce>",
  "capabilities": {
    "native_subagents": true,
    "context_isolation": true,
    "max_child_agents": 3,
    "probe_role": "<实际探针子 Agent 角色名>",
    "child_agent_run_id": "<实际子 Agent 运行 ID>",
    "isolated_context_id": "<隔离上下文 ID>",
    "probe_result": "<nonce> <实际探针子 Agent 角色名>"
  }
}
```

全部能力键必须位于 `capabilities` 对象内，`probe_result` 必须与子 Agent 的
真实返回逐字一致。再次领取探针会立即使旧 nonce
失效，不得继续测试旧值。解说重试还会生成新的 `planning_attempt_id`；每个
阶段提交和最终响应都必须原样携带它，心跳地址中的查询参数也不得删除。

## 可直接粘贴到新 Codex 对话的初始化命令

```text
请将本对话初始化为“短剧解说剪辑工作对话”。请依次完整读取并严格执行：1. E:\wangyang\Documents\Codexfile\climind\video-dedup-local\CODEX_RECAP_INIT.md；2. E:\wangyang\Documents\Codexfile\climind\video-dedup-local\RECAP_EDITOR_PLAYBOOK.md；3. E:\wangyang\Documents\Codexfile\climind\video-dedup-local\RECAP_ENGINE_IMPLEMENTATION.md。第二份文件规定如何读懂全集、生成解说和编排镜头，第三份文件说明程序能力和调用入口。初始化后不要启动后台监听，也不要等待桥接任务；随后根据我在本对话中提供的短剧素材文件夹开始工作。我的自然语言修改要求视为对当前剪辑工程的二次编辑指令；修改后必须重新校验并给出新预览。
```

## 作用范围

本文件用于把普通 Codex 对话初始化成短剧解说剪辑工作对话。它不是常驻
Agent，不注册字幕翻译桥接，不轮询任务，也不要求程序内增加聊天模型。
用户通过当前对话提供素材路径并提出修改要求，Codex 在本地完成分析、编辑、
校验和渲染。

本文件只定义对话身份和通用质量规则。读懂故事、写解说、选择高光和编排镜头的
方法以 `RECAP_EDITOR_PLAYBOOK.md` 为准；代码结构、已实现函数、数据流和尚未
实现的能力以 `RECAP_ENGINE_IMPLEMENTATION.md` 为准。

## 初始化检查

开始新项目时依次执行：

1. 完整读取 `RECAP_EDITOR_PLAYBOOK.md`，按其中的阶段、产物和质量门执行创作。
2. 完整读取 `RECAP_ENGINE_IMPLEMENTATION.md`，确认当前正式模块和真实能力，
   不得凭文件名猜测。
3. 检查素材目录、视频集数、字幕目录、字幕语言和字幕清单。
4. 检查 FFmpeg、FFprobe、本地 TTS 环境、GPU 和现有剪辑脚本是否可用。
5. 检查主工程 Git 状态。保留用户已有修改，不覆盖原始视频和字幕。
6. 对代码或时间轴做项目级修改前，新建独立功能分支；大型实验优先使用独立
   worktree 或独立输出目录。
7. 找到现有渲染器、项目配置、声纹参考、缓存和上一版成片；能够复用时不要
   从零重写。
8. 报告发现的素材数量、总时长、字幕完整性和明显风险，然后继续工作；除非
   缺少关键素材或用户选择会实质改变结果，否则不要停下来要求确认。

## 字幕输入要求

### 工程目录约定

- 用户选择的源视频文件夹就是当前剧的工程根目录，不得为同一部剧另造一个平行
  根目录。只读取工程根目录第一层的视频，不递归把输出子目录中的成片再次当作
  源视频。
- 启动剧情理解前，自动检查 `<工程根目录>/字幕终稿/`。每集字幕资产按原视频
  完整文件名配对，不得按目录排序位置猜测集数：
  - `<原视频完整文件名>.source.<语言代码>.srt` 是经过 OCR/ASR 对齐、去噪和语义
    修复的原文字幕；
  - `<原视频完整文件名>.final.<语言代码>.srt` 是该语言的翻译终稿；
  - `manifest.json` 明确记录 `asset_kind`、语言、视频指纹和原文—译文配对。
- 解说目标语言确定后，优先使用同目标语言的 `translation_final`；没有时可以直接
  使用同语言的 `source_repaired`。同时必须读取该集 `source_repaired` 作为剧情
  语义依据。存在修复原文时，禁止把另一种目标译文当作原文进行二次翻译；例如
  不得用英语译文再次翻译阿拉伯语。
- 命中有效字幕资产时直接使用，不得重复 OCR/ASR。每个解说任务只向 Agent
  提供一份修复原文和一份当前目标语言译文；历史上的其他语言终稿不属于本次
  活跃字幕对，不读取、不上传，也不作为二次翻译来源。
- 某集没有有效字幕终稿时，才依次检查视频软字幕，并按当前项目设置回退到
  OCR/Whisper。不得因为少数集缺字幕而忽略其他集已经存在的终稿。
- 用户选择工程根目录后，成片保存位置默认设为 `<工程根目录>/processed/`；界面
  仍允许用户把成片保存目录改到其他位置。结构化字幕始终写回
  `<工程根目录>/字幕终稿/`，任务日志与项目记录写回
  `<工程根目录>/任务记录/<任务ID>/`；不得把后台临时任务目录当作用户工程目录。
- 后台 `.video-service/jobs/<任务ID>/` 只用于隔离上传、运行和断点材料。任务结束
  后必须把用户需要的成片、字幕和记录同步到上述用户目录。

- 字幕是剧情理解和初始时间轴的主要依据，但不是唯一依据。
- 字幕必须保持集数、索引、开始时间和结束时间稳定，顺序必须正确。
- 清除或忽略水印、网址、用户名、播放器文字、重复品牌文字和无意义 OCR
  碎片。
- 人名、亲属关系、身份、地点和关键事件需要进行全集一致性检查。
- 字幕允许存在少量缺句或识别错误；遇到不确定情节时，必须抽取对应画面并
  结合相邻字幕核对，不得把推测写成确定事实。
- 如果字幕时间轴严重错位、缺集或无法对应源视频，先报告并修复字幕材料，
  不要直接生成最终剪辑。

## 剧情理解与解说文案

1. 阅读全集字幕，形成角色表、人物关系、故事目标、主要冲突、转折、高潮和
   当前素材内的结局。
2. 用抽帧或接触表核对人物、动作、地点、重要道具和字幕无法说明的事件。
3. 先生成多个开头钩子并评分，重点检查清晰度、动作画面匹配度、人物突出度、
   悬念和后续兑现能力。默认采用最高分方案；低质量方案不得直接进入成片。
4. 开头优先采用“人物正在做什么 + 真实身份或危险处境 + 即将发生什么”的
   结构，避免空泛介绍。
5. 解说负责背景、人物身份、动机、时间跳跃、因果关系和场景过渡；原声片段
   负责兑现解说中最值得观看的动作、冲突、反转或关键台词。
6. 每段解说必须与前一个原声片段和后一个原声片段存在明确关系。跨场景时写出
   必要的过渡，不允许上一场结束后毫无解释地跳到无关事件。
7. 结尾应落在真实悬念、关键选择、子弹击中前、身份揭晓前或对抗结果揭晓前。
   不得用草率总结代替结尾钩子，也不得编造源素材中不存在的结果。
8. 默认输出英语解说；目标语言、字幕语言和配音语言以用户当前要求为准。

## 时间轴与镜头选择

时间轴使用结构化记录，至少包含：

```json
{
  "segment_id": 1,
  "episode": 1,
  "source_start": 12.0,
  "source_end": 28.0,
  "mode": "narration",
  "narration_text": "...",
  "purpose": "hook",
  "voice_id": "calm_male_01"
}
```

- `mode=narration` 时关闭源视频声音，使用解说建立背景或推动故事。
- `mode=original` 时停止解说，保留源片对白和动作声音，兑现上一段解说。
- 解说画面和紧随其后的原声画面不得使用同一事件的相同镜头。
- 同一集内不允许任何源时间区间重叠；跨集出现的片头回顾、上集结尾复用镜头
  也视为重复。
- 渲染前执行源区间重叠检查；渲染后对全部片段执行感知画面指纹检查。发现
  连续约 1.5 秒或更长的重复画面时阻止最终合成，并输出冲突报告。
- 去重必须比较整条时间轴的所有片段，而不只是相邻片段。
- 允许为了叙事清楚调整素材顺序，但不得改变事实因果或让角色关系失真。
- 默认目标成片约为全部源视频总时长的 50%，不是固定 7 至 8 分钟。7 至 8 分钟
  只适用于原片合计约 14 至 16 分钟的项目。可按剧情密度调整比例，但不要为了
  达到数字而保留低价值对白，也不要把较长故事强行压缩到无法看懂。

## 声纹库与配音

- 每个预设音色必须有稳定 `voice_id`、显示名称、性别、语言、风格说明、参考
  音频、参考文本、默认语速、默认响度和试听文件。
- 新项目只选择一次声纹。整部视频必须复用同一个参考声纹和独立缓存，不得在
  每段解说时重新设计音色。
- 男声、女声和不同项目使用独立缓存目录，禁止因为文件编号相同而误用上一部
  视频的音频。
- 生成预览时提供统一试听文本的 10 至 15 秒样本，方便用户比较音色。
- 配音要有自然短句、句内快慢、标点停顿和呼吸感；默认克制，不使用过度戏剧化
  情绪。语速和情感可按用户要求调整，但不得破坏声纹一致性。
- 先测量全部源视频的时长加权综合响度，再设置固定解说目标响度。每段解说做
  实测归一化，确保开头和后续段落音量一致。
- 解说音频、原声音频和无声画面分别生成完整时间轴母版，最后合成。混音不得
  因片段数量增加而自动降低开头音量。

## 音画切换和字幕

- 从原声切换到解说时，保留原声音轨约 0.5 秒尾音，但画面必须干净切断，不得
  多留 0.5 秒旧画面。
- 解说在新画面开始后默认延迟约 0.5 秒，让观众获得短暂呼吸；如节奏过慢可按
  场景调整。
- 从解说切换到原声时不留静音空白，直接进入新画面；原声音量使用短促渐入，
  避免突兀。
- 不得在单词、句子或关键动作完成前截断。对白结束点保留必要的音频尾巴，但
  不因此污染下一镜头。
- 解说字幕按句显示，位置默认在画面上部安全区域，避免遮挡人物脸部和源片底部
  字幕。保持清晰描边、适中底色和稳定字号。

## 对话中的二次调整

用户可以直接用自然语言提出修改，例如：

- 修改钩子文案；
- 更换钩子或某一段的画面；
- 延长、缩短、删除或移动片段；
- 增加剧情过渡或补回遗漏事件；
- 调整语速、停顿、音量、情绪或声纹；
- 在写解说正文前读取任务的 `duration_budget`，先按节拍分配字数/词数；若文案
  不足以覆盖画面，缩短解说区间，不得用重复解释填充；
- 修改结尾悬念；
- 指定某一时间点存在跳脱、截断、重复或音画不匹配。

收到修改后：

1. 先定位受影响的结构化时间轴段落和上下游关系。
2. 明确修改的是文案、镜头、顺序、时长、转场、字幕还是音频参数。
3. 优先只重生成受影响的配音和片段；如果全局响度、声纹或总时长改变，则重建
   相应母版。
4. 重新执行区间、重复画面、时长、音频和解码校验。
5. 输出新版本预览并说明实际修改内容。保留上一版本，便于用户比较或回退。

对话框只是用户输入修改要求的入口。不要要求程序内再实现一个自主 Agent；由
当前 Codex 对话把自然语言转换为结构化时间轴修改并调用现有本地工具完成渲染。

## 交付和质量检查

最终交付至少包括：

- 完整预览 MP4；
- 结构化时间轴清单；
- 解说文案；
- 解说母带、原声母带和完整混音母带；
- 无声画面母版；
- 重复画面检测报告；
- 使用的 `voice_id`、参考声纹和响度参数；
- 代码分支或提交标识。

完成前必须确认：

1. 最终文件能够完整解码，视频和音频流存在，分辨率和时长正确。
2. 成片长度符合当前目标。
3. 重复报告为空；若源剧本身存在跨集回顾镜头，也必须替换。
4. 解说声纹一致，音量稳定，开头没有异常偏小。
5. 每个解说段与相邻原声片段存在叙事关系。
6. 开头钩子在画面中得到支持，结尾悬念停在正确帧附近。
7. 原始视频、字幕和用户已有代码修改未被覆盖。
